# Build and test mcl-uwb.
#
# CI does not reimplement the gates. It checks out the tree and runs the same
# CMake build and ctest suite a contributor runs locally, so a green tick here
# and a green run on a laptop mean the same thing. The heavier
# multi-toolchain/sanitizer matrix lives in mcl-core's release-gates workflow,
# not on every pull request.
#
# Policy, enforced by tools/check-publication-readiness.sh and
# tools/go-no-go-audit.sh:
#
#   - least-privilege token: contents: read, nothing else
#   - no pull_request_target, so fork code never runs with our credentials
#   - no secrets reachable from a pull_request trigger

name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    # Sibling source dependencies are checked out from the same organisation.
    # While a repository is PRIVATE that cannot work: the automatic GITHUB_TOKEN
    # is scoped to this repository alone and cannot read a private sibling, and
    # no org secret is used here because a secret reachable from a pull_request
    # trigger is exactly what the CI policy forbids. A valid token CAN read any
    # PUBLIC repository, so this job starts working the moment the repositories
    # become public -- which is when it is needed, since there are no outside
    # contributors before then. It is skipped rather than left to fail red.
    if: github.event.repository.private == false
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        cc: [gcc, clang]
    steps:
      - name: Check out mcl-uwb
        uses: actions/checkout@v4
        with:
          path: mcl-uwb

      # mcl-link is a sibling source dependency: mcl-uwb's CMakeLists reads
      # ../mcl-link directly rather than vendoring a copy, so the checkout
      # layout has to match a developer's working tree.
      - name: Check out mcl-link
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/mcl-link
          path: mcl-link

      - name: Configure
        env:
          CC: ${{ matrix.cc }}
        run: cmake -S mcl-uwb -B build -DCMAKE_BUILD_TYPE=Debug

      - name: Build
        run: cmake --build build -j"$(nproc)"

      - name: Test
        run: ctest --test-dir build --output-on-failure
